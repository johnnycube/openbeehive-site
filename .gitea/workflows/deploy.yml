# Gitea Actions — build the site on every branch and PR, deploy on release tags.
# svelte-check + the static build are the sanity check. Deployment runs ONLY
# when a v* tag (e.g. v0.1.0, always tagged on main) is pushed. The deploy job
# is imported from johnny/build and receives the built site as an artifact -
# it never sees this repo's source.
#
# Secrets needed (user-level JOHNNY_ALL_INKL_* plus per-repo):
#   JOHNNY_ALL_INKL_SSH_HOST, JOHNNY_ALL_INKL_SSH_USER,
#   JOHNNY_ALL_INKL_PRIVATE_KEY, JOHNNY_ALL_INKL_HOST_KEY,
#   DEPLOY_REMOTE_PATH (absolute remote directory to rsync into, with a
#   trailing slash, e.g. /www/htdocs/<account>/openbeehive.org/)

name: Check and deploy

on:
  push:
    branches: ['**']
    tags: ['v*']
  pull_request:
  workflow_dispatch:

# Cancel superseded branch/PR builds; never cancel a tag deploy
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}

# Principle of least privilege
permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run check

      - name: Build site
        run: npm run build

      - name: Upload built site
        # NOT actions/upload-artifact: the official v4 action detects Gitea
        # as GHES and aborts. This fork is the same action with that check removed.
        uses: https://github.com/christopherhx/gitea-upload-artifact@v4
        with:
          name: site
          path: build/
          retention-days: 1
          if-no-files-found: error

  deploy:
    needs: build
    # Deploy only on v* release tags — never from branches or PRs.
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    uses: johnny/build/.gitea/workflows/deploy-static-page.yaml@v9
    with:
      artifact-name: site
    secrets:
      SSH_SERVER: ${{ secrets.JOHNNY_ALL_INKL_SSH_HOST }}
      SSH_USER: ${{ secrets.JOHNNY_ALL_INKL_SSH_USER }}
      SSH_PRIVATE_KEY: ${{ secrets.JOHNNY_ALL_INKL_PRIVATE_KEY }}
      SSH_HOST_KEY: ${{ secrets.JOHNNY_ALL_INKL_HOST_KEY }}
      DEPLOY_REMOTE_PATH: ${{ secrets.DEPLOY_REMOTE_PATH }}
